# Punk Records Inference — placeholder Dockerfile (Phase 2)
#
# Pin vLLM base image + apply patches/ + COPY plugin before publishing.
# See docs/SHIP_PLAN.md §6.

# ARG VLLM_BASE=...
# FROM ${VLLM_BASE}
# COPY patches/ /opt/patches/
# RUN python /opt/patches/apply_patches.py
# COPY . /opt/pri-repo
# ENV PYTHONPATH=/opt/pri-repo
# ENTRYPOINT ["/opt/pri-repo/docker/start.sh"]
